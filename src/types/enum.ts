/**
 * Centralized enums for the application
 */

// Authentication types
export enum AuthType {
  USER = 'user',
  CLIENT = 'client',
  ADMIN = 'admin'
}

// User role types
export enum RoleType {
  ADMIN = 'admin',
  MANAGER = 'manager',
  CLIENT = 'client'
}
